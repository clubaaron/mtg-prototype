<%- include('globals/head') %>
<main>
    <article class="card">
        <h1><%= card.name %></h1>
        <img src="<%= card.imageUrl %>" height="331px" width="223px" />
        <p><%= card.text %></p>
        <p><%= card.set %></p>
    </article>
</main>
